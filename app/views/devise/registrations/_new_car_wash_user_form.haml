.container
  #login
    = form_for(resource, as: resource_name, :url => user_registration_path(resource), :html => {:class => "form-signin"}) do |f|
      - if devise_error_messages?
        %div#error_explanation
          %h4= t('errors.messages.registration_part1')
          %h4= t('errors.messages.registration_part2')
      %fieldset
        %div
          = f.label :email do
            = User.human_attribute_name("email")
            %span.red &nbsp;*
          %br/
          = f.email_field :email, :autofocus => true, class: "form-control", required: "required"
        %div
          = f.label :phone do
            = User.human_attribute_name("phone")
            %span.red &nbsp;*
          %br/
          = f.phone_field :phone, class: "form-control", required: "required"
        %div
          = f.label :contact_person do
            = User.human_attribute_name("contact_person")
            %span.red &nbsp;*
          %br/
          = f.text_field :contact_person, class: "form-control", required: "required"
        %div
          = f.label :car_wash_title do
            = User.human_attribute_name("car_wash_title")
            %span.red &nbsp;*
          %br/
          = f.text_field :car_wash_title, class: "form-control", required: "required"
        %div
          = f.label :address do
            = User.human_attribute_name("address")
            %span.red &nbsp;*
          %br/
          = f.text_field :address, class: "form-control", required: "required"
        %div
          = f.label :password do
            = User.human_attribute_name("password")
            %span.red &nbsp;*
          %br/
          = f.password_field :password, class: "form-control", required: "required"
        %div
          = f.label :password_confirmation do
            = User.human_attribute_name("password_confirmation")
            %span.red &nbsp;*
          %br/
          = f.password_field :password_confirmation, class: "form-control", required: "required"
          %br/
        = hidden_field_tag 'user[normal]', false
        #agreement_text
          = render 'agreement'
        %div
          %label.checkbox
            = check_box_tag 'agreement', '', false
            Нажимая кнопку «Зарегистрироваться», я принимаю условия Пользовательского соглашения
        %div= f.submit t("devise.sign_up"), disabled: true, class: "btn btn-lg btn-primary btn-block"
        %br
        = render "devise/shared/links"
